server:
  port:8080
spring:
    datasource:
       driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect:  org.hibernate.dialect.MySQLDialect
      show-sql: true
spring-doc:
  paths-to-match:
    - /api/**/v1/**
  swagger-ui:
    use-root-path: true